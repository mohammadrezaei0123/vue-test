<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/back12"
        tools:context=".ForgetPasswordActivity">

    <LinearLayout android:id="@+id/forgetLinear"
            android:layout_width="0dp"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  app:layout_constraintTop_toTopOf="parent"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent"
                  app:layout_constraintBottom_toTopOf="@id/enterForgetButton"
                  android:gravity="center">
        <TextView android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
        android:text="@string/forget_enter_string"
        android:textColor="@color/hint_color"
        android:textSize="@dimen/text_login_size"/>

        <AutoCompleteTextView android:id="@+id/passworduserForgetTextView"
                              android:layout_width="match_parent"
                              android:layout_height="wrap_content"
                              android:layout_marginTop="20dp"
                              android:layout_marginStart="20dp"
                              android:layout_marginEnd="20dp"
                              android:textSize="@dimen/text_login_size"
                              android:textColor="#fff"
                              android:shadowColor="@color/black_p1"
                              android:outlineAmbientShadowColor="@color/black_p1"
                              android:textColorHint="@color/black_p1"
                              android:textColorHighlight="@color/black_p1"
                              android:textColorLink="@color/black_p1"
                              android:outlineSpotShadowColor="@color/black_p1"
                              android:inputType="textEmailAddress"
                              android:gravity="right"
                              android:hint="@string/email_string"/>

    </LinearLayout>
    <Button android:id="@+id/enterForgetButton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="ارسال و بازگشت"
            android:textSize="@dimen/text_login_size"
            android:background="@drawable/button_shape"
            android:layout_margin="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

</android.support.constraint.ConstraintLayout>